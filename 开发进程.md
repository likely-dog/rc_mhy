# API 通知中转服务开发进程

更新时间：2026-09-01（Asia/Shanghai）

## 当前状态

状态：实现与验收已完成。

按照任务要求，发现指导文档中仍有未决项或相互冲突之处时曾先停止实现并记录问题；用户确认 12 项建议作为实现基线后，已完成 Phase 1 至 Phase 5。

## 最终完成情况

- 已实现 FastAPI 三接口、原子幂等、SQLite WAL 仓储、Redis Streams consumer、YAML 严格渲染、错误分类、指数退避、DLQ、stale recovery 和结构化日志。
- 已完成 Dockerfile、三服务 Compose、共享 SQLite/Redis volumes、健康检查与 worker 自动重启。
- 已完成 45 项测试；45 passed，总覆盖率 77.36%。
- 已完成真实 Docker 冒烟：三容器 healthy，通知从 202 pending 到 worker 单次投递 success，HTTP status 200。
- 已完成低并发及 10 并发入口基准、幂等命中和未知 vendor 契约验证。
- 已完成 README、设计说明、AI 使用说明和 `docs/test-report.md`。
- 测试中的失败、修正及最终数据均记录在测试报告中。

## 已完成

- 已完整阅读 `AI Coding 作业.pdf`，确认原始作业重点是系统边界、可靠性与失败处理、工程取舍、MVP 实现和 AI 使用说明。
- 已阅读 `prd_demo.md`，用于理解方案演进和早期约束。
- 已阅读 `prd_final.md`，确认后续应以 final PRD 为主要实现依据。
- 已检查当前目录：除上述三份输入文档外，原先没有工程代码或配置。
- 已对 final PRD 的依赖白名单、API 契约、数据模型、分发流程、重试策略、分阶段 AC 和附录待完善项进行交叉核对。

## 原计划阶段（均已完成）

- Phase 1：项目骨架、配置、日志、数据库、基础 API、Docker Compose、API 测试。
- Phase 2：供应商 YAML、幂等表和 24 小时清理、并发幂等测试。
- Phase 3：Redis Streams、worker consumer、模板渲染、HTTP 分发、消息回收。
- Phase 4：错误分类、重试、DLQ、stale recovery。
- Phase 5：端到端测试、覆盖率、README、设计文档和 AI 使用说明。
- Docker 实机验收、测试与覆盖率检查。

## 实现前需要确认的问题

### 1. SQLite 跨容器写入方案

final PRD 附录 #6、#17 明确标记为“仍需决策”。API 与 worker 会同时写一个 SQLite 文件，存在 `database is locked` 风险。

建议默认方案：采用 SQLite WAL、`busy_timeout=5000`、SQLAlchemy `NullPool`，并通过同一 named volume 共享 `/data/app.db`；在 README 明确其只适合低吞吐 MVP。若要求更高并发，应改 PostgreSQL，但这会超出当前依赖白名单和 MVP 边界。

### 2. `POST /notify` 的 100ms 指标口径

正文将 `≤100ms` 写成硬约束，但没有指定硬件、压测工具或统计口径；附录 #7 也指出当前无法稳定验收。

建议默认方案：定义为“本地基准环境下目标 p99 ≤100ms，尽力而为”，补一个可复现的轻量测量脚本或测试记录。若必须作为硬 SLA，需要先给定环境、并发量、样本量和是否包含 Redis/磁盘 I/O。

### 3. 延迟重试的持久化程度

正文推荐 `asyncio.sleep(delay)`，但 worker 重启会丢失内存中的定时动作；附录 #10 同时给出“接受降级”和“Redis Sorted Set scheduler”两条路线。

建议默认方案：MVP 使用 `asyncio.sleep`，由数据库中的 `next_retry_at` 和每 30 秒 stale recovery 兜底，并在 README 明确重启可能增加最多约 30 秒延迟。Sorted Set scheduler 作为未来演进，不在本次实现。

### 4. vendor 配置在入队后缺失时的行为

正文伪代码直接索引 vendor，可能把 `KeyError` 当作可重试异常；这与 DLQ 已定义的 `vendor_config_missing` 原因冲突。

建议默认方案：按附录 #12 修正为不可重试，单次处理后进入 DLQ，reason 为 `vendor_config_missing`，并增加测试。

### 5. `event_type` 与 payload 校验边界

测试指导要求“payload 不符合 vendor schema → 400”，但 YAML 契约没有 payload schema；附录 #14 建议新增可选 `event_types`，这会改变原配置契约。

建议默认方案：保持 payload 只校验为 JSON object；YAML 增加可选 `event_types`，配置了才校验 event type。模板字段缺失只能在 worker 渲染时发现并进入 DLQ，不能在 API 层可靠返回 400。完整 JSON Schema 校验留作未来演进。

### 6. 幂等命中 dead 状态的 HTTP 语义

附录 #18 给出两种互斥选择：继续返回 200，或改为 409。

建议默认方案：保持正文 API 契约，返回 `200` 和当前 `dead` 状态；调用方如需重投应使用新 key。这样不擅自改变已声明的接口契约。

### 7. 其余 4xx 的错误分类

正文仅枚举部分 4xx，但总原则又写明“除 408、429 外的 4xx 不重试”。

建议默认方案：以总原则为准，所有 `400 <= status < 500` 中仅 408、429 可重试，其余全部不可重试。

### 8. 重试次数的 off-by-one

伪代码先把数据库 attempts 加一，却继续用旧对象的 `n.attempts` 判断是否达到上限并计算退避，会导致首轮延迟和最终尝试次数口径不一致。

建议默认方案：`attempts` 表示“已经实际发出的请求次数”；本轮使用 `current_attempt = n.attempts + 1`，先持久化该值。失败时若 `current_attempt >= MAX_ATTEMPTS` 则进入 DLQ，否则下一次延迟按 `BASE_DELAY * 2 ** (current_attempt - 1)` 计算，使第一次失败后的基础延迟为 1 秒，总 HTTP 尝试次数严格为 10。

### 9. `last_http_status` 的写入时机

数据模型定义了字段，但正文没有规定各状态分支何时更新。

建议默认方案：收到任何 HTTP 响应后，无论成功、重试或死信，都写入该响应码；超时、连接错误和渲染错误写 `NULL`。每次状态变更同时更新 `updated_at`。

### 10. E2E mock 的进程边界

Phase 5 要求用 respx，但 Docker 中的 worker 是独立进程，进程内 respx 无法拦截它的 HTTP 请求；附录 #24 已指出这一冲突。依赖白名单也不允许直接加入 aiohttp 作为 fake server。

建议默认方案：把 E2E 分为两层：pytest 中在同一进程运行 consumer/dispatch 并使用 respx，验证完整业务链路；Docker Compose 演示另用 Python 标准库实现的最小 fake HTTP server，或只做人工可配置端点演示。这样不增加依赖，也不虚构跨进程 mock 能力。

### 11. 覆盖率工具未列入依赖白名单

DoD 要求执行 `pytest --cov=app`，但 dev 依赖没有 `pytest-cov`，因此该命令在干净环境中不能工作。ruff 也不能提供类型检查，但 Phase 5 AC 同时要求“无 type warning”。

建议默认方案：将 `pytest-cov` 加入 dev 依赖，并在 README 记录它是满足明确覆盖率验收命令所必需的额外依赖；类型验收限定为 Python 编译检查、ruff 和测试，不额外引入 mypy/pyright。若严格禁止任何白名单外依赖，则只能改用 Python `coverage` 自行安装或取消 `--cov` 命令，两者都与现有 DoD 冲突。

### 12. 提交历史要求

DoD 要求至少 5 个语义清晰的 git commit，但当前目录是否应由本次过程直接初始化和提交、以及 `{your_nickname}` 的实际值均未提供。

建议默认方案：先在当前目录完成工程，不代替用户创建 GitHub 仓库或提交；项目标题暂用通用名称。待提供 nickname 后再调整仓库展示名称。若需要我创建分阶段 commit，需要明确授权。

## 建议的执行基线

若以上建议均可接受，后续按 final PRD 的 Phase 1 → Phase 5 顺序实现，并采用以下优先级处理冲突：

1. 原始作业 PDF 的目标与评估重点。
2. final PRD 正文的 MUST/MUST NOT 和 API 契约。
3. final PRD 附录对已识别缺陷的修正建议。
4. demo PRD 仅作背景，不覆盖 final PRD。

上述 12 项建议已获用户确认并作为最终实现基线。当前没有未完成的业务实现项；GitHub 仓库命名和提交历史仍由用户在提供 nickname 后处理，本次未擅自创建远程仓库或 commit。
